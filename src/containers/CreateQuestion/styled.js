import styled from 'styled-components';
import { DEFAULT_PADDING } from '../../constants/';

export const StyledCreateQuestion = styled.div`
  padding: ${DEFAULT_PADDING};
  display: flex;
  flex-direction: column;
`;
